# 决策、未解决问题与冲突

> 上级入口：[00_INDEX.md](00_INDEX.md)
> 用途：拆分自旧 `_HANDOFF.md` 第 5、6、15 章。
> 权威 ID：D / Q / X
> 不存放：事实、进度、产物、风险和行动清单。
> 更新时间：2026-09-23
> 迁移说明：旧章节编号 (5, 6, 15)；完整映射见 `00_INDEX.md`。

## 5. 关键决策与理由

- [D-001] [P0] [已确认] 采用「方案 C」：先定义数据源抽象 `IPrimoCacheSource`，第一期用 `rxpcc.exe` CLI 取数，未来可替换为驱动直读（`\\.\FancyCcV` + IOCTL）。
  - 原因：官方没有 SDK，也没有 JSON/XML 输出；CLI 是唯一官方脚本化途径；直读 IOCTL 控制码未公开，需逆向 `rxfcv.sys`，成本过高。
  - 备选方案：A 直接 CLI（无抽象层）、B 驱动 IOCTL 直读、D 混合。
  - 放弃备选方案的原因：A 不利于后续替换；B 成本/风险过高（未公开控制码）；D 无必要。
  - 来源：[S-002]（旧会话 15:43 回复：SDK 调研结论）
  - 状态：有效
  - 被替代关系：无

- [D-002] [P0] [已确认] 命中率平滑采用「方案 A：30 秒滚动窗口累计比值」（`kHitRateWindowMs = 30000`，即 6 个采样）。
  - 原因：物理意义明确（最近 30 秒整体命中率），小样本自动被摊平；无读取区间只是不贡献分子分母，数值不会被打断。
  - 备选方案：B EWMA 指数平滑、C 最小样本门槛、D 限制变化速率。
  - 放弃备选方案的原因：B 「最近多久」不直观；C 单独用不解决打断问题（见 [Q-003] 仍可作为补充）；D 数值会失真，不适合监控。
  - 来源：用户原话（旧会话 15:45「选择方案A」）+ 助手方案说明（[S-002]，15:44）
  - 状态：有效（已实现：提交 `7b1c759`）
  - 被替代关系：取代 [D-009]

- [D-003] [P0] [已确认] PrimoCache 显示项最终只保留 3 个：`PC_HIT_SPD`(命中:)、`PC_MISS_SPD`(未中:)、`PC_HIT_RATE`(命中率:)。
  - 原因：`读取 = 命中 + 未中` 属冗余推导量；读写切换项与内置项信息重合度低但用户不需要；动态标签功能失效。
  - 备选方案：保留 5 项（原始实现）；保留 4 项（去掉读取）。
  - 放弃备选方案的原因：用户明确「删除 4 我不需要这个数据」「不需要读取速度」。
  - 来源：用户原话（旧会话 08:21、08:41）+ 助手回复（[S-002]，13:19）
  - 状态：有效
  - 被替代关系：取代 [D-008]

- [D-004] [P1] [已确认] 采用惰性采样：只有当显示项被主程序绘制时才每 5 秒采样，空闲 30 秒后完全停止（`kIdleTimeoutMs`）。
  - 原因：用户要求可评估性能开销；实测每 5 秒一次 rxpcc ≈ 0.9% 时间 / 0.3% CPU，空闲时可降到零。
  - 备选方案：无论是否显示都持续采样（原实现）。
  - 放弃备选方案的原因：无意义开销；用户选择了「惰性采样」。
  - 来源：用户原话（旧会话 12:34「1 可以采用惰性采样」）+ 助手实现（[S-002]，12:42）
  - 状态：有效
  - 被替代关系：无

- [D-005] [P1] [已确认] 失败处理：单次采样失败视为抖动沿用旧值，连续 2 次失败才置无效；无效状态统一显示 `--`，全文移除 `N/A`。
  - 原因：用户要求「2次以上失败才进行显示 --，不要显示 NA」。
  - 来源：用户原话（旧会话 08:50）+ 助手实现（[S-002]，08:54）
  - 状态：有效
  - 被替代关系：取代早期的「单次失败即 N/A」

- [D-006] [P1] [已确认] 版本与发布策略：bug 修复 + 平滑方案合并发布为 `v1.2.1`；用户先要求「只标版本不执行」，随后以「执行 A1」明确授权发布。
  - 原因：用户想控制发布时机（先把平滑做好），再统一发布。
  - 来源：用户原话（旧会话 15:47、15:51、16:45「执行 A1」）+ 助手发布结果（[S-002]，17:01）
  - 状态：有效（已发布 v1.2.1，Latest）
  - 被替代关系：取代「v1.2.1 未发布」的早期状态描述（见 [X-001]）

- [D-007] [P2] [已确认] 构建产物按目标平台命名（工程级 `TargetName`）：x64 → `taskbardynamic-x64.dll`，x86 → `taskbardynamic-x86.dll`。
  - 原因：两个位数同名 DLL 无法区分；用户要求同时提供 x32 版本。
  - 来源：用户原话（旧会话 07:11「同时提供一个 x32 的版本」）+ 助手实现（[S-002]，07:22）
  - 状态：有效（提交 `ea3452f`）
  - 被替代关系：取代早先都叫 `taskbardynamic.dll` 的做法

- [D-008] [P2] [已废弃] 早期实现包含 5 个 PrimoCache 显示项（`PC_READ_SPD`、`PC_HIT_SPD`、`PC_MISS_SPD`、`PC_RW_SPD` 读写切换、`PC_HIT_RATE`）以及 `DynamicInfo` 的动态标签回调。
  - 原因：初版按「信息尽量全」实现。
  - 来源：[S-002]（旧会话 08:01、08:16 回复）
  - 状态：已废弃
  - 被替代关系：被 [D-003] 取代（读取项、读写切换项、动态标签功能全部删除）

- [D-009] [P2] [已废弃] 早期命中率口径为「最近 5 秒区间的 ΔCached/ΔTotal，1 位小数」。
  - 原因：最直观的瞬时命中率。
  - 来源：[S-002]（旧会话 08:16 回复）
  - 状态：已废弃
  - 被替代关系：被 [D-002] 取代（改为 30 秒滚动窗口）

- [D-010] [P2] [已废弃] 早期发布与标签：`v1.0`（标签 `release`）Release，以及附件命名 `taskbardynamic.dll`。
  - 原因：历史遗留（v1.0 附件曾被 v1.1 DLL 覆盖，无法从远端找回）。
  - 来源：[S-002]（旧会话 07:10「删除 V1.0 Release」、07:11 回复）
  - 状态：已废弃（v1.0 Release 已删除；`release` 标签已从本地和远端删除）
  - 被替代关系：被 [D-007] 与 v1.1/v1.2/v1.2.1 取代
## 6. 未解决问题与待确认事项

- [Q-001] [P0] [待确认] README 更新日志与实际发布状态矛盾：`README.md` L414 仍写 `### v1.2.1（未发布）`、L416 写「**尚未发布**（等命中率平滑方案完成后一并发布）」，但 v1.2.1 早已发布且为 Latest。
  - 为什么重要：这是用户明确提出的待办「README 文档修复一下」，且文档会误导使用者。
  - 当前阻塞：旧会话卡死（[R-001]），该需求从未被执行；需在新会话处理。
  - 可选方案：A 只改这两处文字；B 顺带核对整篇 README 与当前实现/发布状态是否一致。
  - 推荐默认值：A + 最小核对（L414/L416 必须改）。
  - 需要谁确认：用户（是否顺带通篇核对）。
  - 来源：用户原话（旧会话 17:39「README 文档修复一下」）+ [S-005]

- [Q-002] [P0] [待确认] 工作区 2 个未提交修改（`README.md`、`taskbardynamic/TaskBarDynamic.cpp`）如何处置：提交 / 修改 / 丢弃？
  - 为什么重要：仓库处于「本地 ≠ 远端」的中间态，且已用这些改动重新编译过（本地 DLL 与 v1.2.1 附件哈希不同）。
  - 当前阻塞：等待用户确认改动来源与意图（见 [F-014]）。
  - 可选方案：A 用户确认后按显式路径提交并推送；B 继续保留在本地；C 丢弃。
  - 推荐默认值：先不动，等用户确认。
  - 需要谁确认：用户。
  - 来源：命令 `git status` / `git diff`（[S-007]）

- [Q-003] [P1] [待确认] 是否实现「B4-b 命中率显示门槛」：窗口内累计读取量 < 1MB 时继续显示 `--`（拟新增 `kMinHitRateReadBytes = 1024 * 1024`）。
  - 为什么重要：避免空闲后单次 4KB 读取把命中率直接显示成 0.0% / 100.0%。
  - 当前阻塞：用户未拍板（旧会话最后一轮已卡死）。
  - 可选方案：B4-a 按「窗口内至少 2 个区间有读取」；B4-b 按「累计读取量 ≥ 阈值」（助手推荐）。
  - 推荐默认值：B4-b，阈值 1MB。
  - 需要谁确认：用户。
  - 来源：[S-002]（旧会话 17:04 回复）

- [Q-004] [P1] [待确认] 「这个插件还有没有什么值得优化的地方？」（旧会话 17:12 提问）尚无任何回答。
  - 为什么重要：这是用户最后一次真正的技术提问。
  - 当前阻塞：该回合起会话彻底卡死（[R-001]）。
  - 推荐默认值：在新会话基于当前代码给出优先级清单（含代价与风险），不直接改代码。
  - 需要谁确认：用户（是否要我直接动手）。
  - 来源：用户原话（旧会话 17:12）[S-001]

- [Q-005] [P1] [待确认] 本机 `C:\tool\TrafficMonitor\config.ini` 的三处手工改动是否已完成：`tbar_display_item` 520 → 8、`plugin_display_item` → `mq72ZrBHN2,DE7etVv1nR,PC_HIT_RATE`、`PC_MISS_SPD` 标签 → `未中:`。
  - 为什么重要：影响用户任务栏实际显示效果（标签以 config 缓存为准）。
  - 当前阻塞：约束 [C-002] 禁止助手代改，只能用户自改。
  - 需要谁确认：用户。
  - 来源：[S-002]（旧会话 13:22 / 13:48 回复）

- [Q-006] [P2] [待确认] 可选任务 A2：是否把 `v1.2` Release 的附件也换成修复版（当前 v1.2 附件仍是修复前构建）。
  - 为什么重要：v1.2 附件含「千位分隔符解析」等潜在缺陷（当前 rxpcc 输出不带分隔符，暂不发作）。
  - 推荐默认值：不做（v1.2.1 已是 Latest），仅提示用户改用 v1.2.1。
  - 需要谁确认：用户。
  - 来源：[S-002]（旧会话 16:44 回复的 A2 项）

- [Q-007] [P1] [待确认] 旧会话 `01a0cb30-827c-7fe1-848f-e3b5d68dae2e` 如何处置（建议归档、不再发送消息），以及后续工作在新会话还是本会话进行。
  - 为什么重要：避免再次把时间浪费在一个发不出请求的会话上。
  - 推荐默认值：归档旧会话；后续工作在干净的新会话进行（先读本文件）。
  - 需要谁确认：用户。
  - 来源：本会话用户原话「你去聊天是无法成功的，上下文已经炸了」+ [R-001]

- [Q-008] [P2] [待确认] 本地 git 标签缺少 `v1.2.1`（远端 Release 存在）。是否需要 `git fetch --tags` 补齐。
  - 为什么重要：影响本地版本核对与后续发布脚本。
  - 推荐默认值：需要时执行 `git fetch --tags`（需网络）。
  - 需要谁确认：用户。
  - 来源：命令 `git tag --list`（[S-007]）+ [S-002]

- [Q-009] [P2] [待确认] 是否需要保留/整理 `C:\tool\TrafficMonitor\plugins` 下的 4 个历史备份 DLL（`.bak`，见 [F-020]）。
  - 为什么重要：这些是部署回滚的唯一依据（尤其 2025-06-17 的原始版本）。
  - 推荐默认值：保留，不动。
  - 需要谁确认：用户。
  - 来源：[S-010]

- [Q-010] [P2] [待确认] 本会话早先创建的中间文件 `D:\coding\taskbardynamic\_handoff\2026-09-23-traffic-monitor.md` 的处置。
  - 为什么重要：用户要求「不要创建多个散落文件，全部内容统一写入 `CONTEXT\_HANDOFF.md`」。
  - 推荐默认值：内容已并入本文件，删除该目录（本文件写入完成后执行）。
  - 需要谁确认：用户（若不同意保留则删除）。
  - 来源：本会话用户原话（[S-003]）+ [S-011]
## 15. 冲突与覆盖关系

- [X-001] 冲突 ID：README 的发布状态 vs 实际发布状态
- 冲突内容：`README.md` L414 写「### v1.2.1（未发布）」、L416 写「**尚未发布**（等命中率平滑方案完成后一并发布）」；但 GitHub 上 `v1.2.1` 已发布且为 **Latest**（标签→`261213e`），附件也校验过。
- 双方来源：README 文件（[S-005]）；发布记录（[S-002] 旧会话 17:01 回复，[A-010]）
- 无法确定的原因：README 是「实现平滑方案前」写的状态描述，之后用户直接下达「执行 A1」发布，但文档未同步（该回合之后旧会话即卡死）
- 暂时采用的默认处理：以**发布事实**为准；把 README 两处当作待修缺陷（[Q-001]/[T-003]）
- 需要用户确认的问题：是否顺带通篇核对 README 与当前实现的其它差异（例如「最近 5 秒区间」类残留表述是否已全部消除）

- [X-002] 冲突 ID：构建平台名 `Win32` vs `x86`
- 冲突内容：README 历史写法给出 `msbuild ... /p:Platform=Win32`（会报 `MSB4126`）；工作区 README 已改为 `x86`；而 vcxproj 内部配置名确实是 `Win32`。
- 双方来源：`taskbardynamic.sln`（`Debug|x86 = Debug|Win32`）与 `taskbardynamic.vcxproj`（[S-006]）；README L74（[S-005]）
- 无法确定的原因：不是事实冲突，而是**作用域不同**（解决方案层 vs 工程文件层）
- 暂时采用的默认处理：**以解决方案层 `x86` 作为命令行参数**（[F-012]）；文档中若提到工程文件平台名则写 `Win32` 并注明对应关系
- 需要用户确认的问题：无（已澄清）

- [X-003] 冲突 ID：未提交改动的作者与意图不明
- 冲突内容：`README.md`(mtime 18:08) 与 `TaskBarDynamic.cpp`(mtime 18:37) 有未提交修改，但旧会话在 17:12 之后没有任何工具调用记录；同时用户多次表示「我自己改」。
- 双方来源：rollout 无 `function_call`（[S-009]/[S-002]）；文件 mtime 与重新编译的产物时间（[S-010]）；用户原话「你不用修改了，待会我自己修改」（[S-001]）
- 无法确定的原因：无法证明是用户手改还是其他未记录进程/工具写入
- 暂时采用的默认处理：按「用户手工改动」对待（[F-014] 标 [推断]），**不提交、不覆盖、不删除**
- 需要用户确认的问题：这两处改动是否由你完成？是否已完成？是否要提交？（[Q-002]）

- [X-004] 冲突 ID：同一时刻存在两个「当前 DLL」
- 冲突内容：本机**已部署**的 `plugins\taskbardynamic-x64.dll` = `03338161…`（= v1.2.1 发布附件）；工作区**刚编译**的 `x64\Release\taskbardynamic-x64.dll` = `AE62E841…`（71,168B，含未提交改动）。
- 双方来源：[S-010]（哈希与时间戳）、[S-002]（发布校验）
- 无法确定的原因：两者都「存在且有效」，但语义不同（已部署 vs 最新构建）
- 暂时采用的默认处理：任何「与发布附件一致」的陈述必须指明是**已部署文件**；核对时以哈希为准
- 需要用户确认的问题：无（仅需在后续操作时保持区分）

- [X-005] 冲突 ID：v1.2 附件与「修复后构建」
- 冲突内容：`v1.2` Release 的附件仍是**修复前**构建（x64 `5c8ca5bc…`），而 v1.2.1 是修复后构建（x64 `03338161…`）；若用户或文档把 v1.2 当作「已修复」，会与实际不符。
- 双方来源：[S-002]（旧会话 16:44「v1.2 的附件仍是修复前构建」与 17:01 发布校验）
- 无法确定的原因：并非事实冲突，而是历史产物状态需要显式记录
- 暂时采用的默认处理：默认不改 v1.2 附件（[Q-006] 的 A2），对外推荐使用 v1.2.1
- 需要用户确认的问题：是否需要把 v1.2 附件替换为修复版（默认不做）

- [X-006] 冲突 ID：「插件默认不开启」vs 用户配置里实际是开启的
- 冲突内容：用户原话「插件默认这些选项都不是开启的」（意图：插件不应替用户勾选）；但助手在用户机器上观测到 `plugin_display_item` 里确实包含 `PC_HIT_RATE,PC_HIT_SPD,PC_MISS_SPD`（即当时是开启状态）。
- 双方来源：用户原话（[S-001]，13:04）；助手观测与给用户的自改建议（[S-002]，13:22）
- 无法确定的原因：「默认」指插件新注册项的初始状态，而用户 `config.ini` 是用户自己设置的结果，两者不是同一层面
- 暂时采用的默认处理：坚持 [C-001]/[C-002]：插件只注册，**不代改用户勾选**；如需变更由用户自己在 `config.ini` 改并重启
- 需要用户确认的问题：`config.ini` 三行是否已按 [P-005] 改完（[Q-005]）

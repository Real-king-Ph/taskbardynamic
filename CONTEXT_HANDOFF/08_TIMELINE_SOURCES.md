# 时间线与原始来源索引

> 上级入口：[00_INDEX.md](00_INDEX.md)
> 用途：拆分自旧 `_HANDOFF.md` 第 12 章与第 14 章。
> 权威 ID：时间线 / S
> 不存放：决策正文、事实正文、风险和产物详情。
> 更新时间：2026-09-23
> 迁移说明：旧章节编号 (12, 14)；完整映射见 `00_INDEX.md`。

## 12. 时间线与变更记录

> 说明：rollout 内时间戳为 UTC，本表换算为本地时间（Asia/Shanghai = UTC+8）。

| 时间或轮次 | 动作 | 结果 | 影响 | 来源 |
| ----- | -- | -- | -- | -- |
| 2026-09-23 06:15 | 用户「帮我扫描一下这个项目」 | 完成项目体检报告（三层架构、3 个显示项、17 个受版本控制文件、缺 README/LICENSE） | 确立项目基线 | [S-001]/[S-002] |
| 06:18 | 用户要求优化代码 + 生成文档 | 修 4 个真实 bug（NaN、`999.KB/s` 截断、过期极值、首次采样伪极值）+ 工程配置统一 C++17/`/MP` + 补 README；版本 → v1.1 | 代码质量与文档到位 | [S-001]/[S-002] |
| 06:29 | 用户要求用正在运行的 TrafficMonitor 实机跑一次 | 定位宿主为 `C:\tool\TrafficMonitor\`，备份旧 DLL 并部署新构建，重启后验证已加载（哈希一致） | 建立「真机验证」流程 | [S-001]/[S-002] |
| 07:10 | 「新增 release V1.1」/「删除 V1.0 Release」 | v1.1 发布为 Latest；v1.0 Release 删除（其附件曾被 v1.1 DLL 覆盖，无法找回） | 发布流程落地 | [S-001]/[S-002] |
| 07:11-07:18 | 「同时提供一个 x32 的版本」+ 同步本地 DLL | 发布 x86 附件并改名 `taskbardynamic-x64.dll`/`taskbardynamic-x86.dll`；删除孤儿标签 `release` | 双位数产物规范确立 | [S-001]/[S-002] |
| 07:22 | 编译产物命名固化 | 在 vcxproj 设置 `TargetName`（4 配置），README 同步升级提示（先删旧 `taskbardynamic.dll`）；提交 `ea3452f` 并推送 | 防止再次出现命名不一致 | [S-002]/[S-007] |
| 07:23-07:24 | 用户询问 primocache 命令 | 输出 `rxpcc.exe` 4.4.0 命令总表与 `perf` 输出格式说明 | 为取数方案打基础 | [S-001]/[S-002] |
| 07:25-07:27 | 用户要求显示 4 项并「先给方案」 | 给出 4 个方案（A 后台线程调 CLI / B 驱动直读 / C 抽象层 / D 混合） | 方案讨论基线 | [S-001]/[S-002] |
| 07:34 | 「采用方案C」+ 环境门控 + 关注性能开销 | 调研 SDK（无官方 SDK，IOCTL 未公开）+ 实测 rxpcc 开销（约 44ms/16ms） | 确认 [D-001] | [S-001]/[S-002] |
| 08:01 | PrimoCache 集成实现完成 | 新增 `IPrimoCacheSource`/`RxpccSource`/采样器 + 5 个显示项；版本 → v1.2；修掉 `std::thread` 析构导致的 `0xC0000409` | 核心功能落地 | [S-002] |
| 08:16-08:21 | 用户列 5 项数据决定去留，删除第 4 项 | 删除读写切换项 + 动态标签功能 | 收敛为 3 项（[D-003]） | [S-001]/[S-002] |
| 08:32-08:41 | 用户要求生成手动计划、宣布不需要读取速度 | 生成 `NEXT-STEPS.md`；保留 命中/未命中/命中率 | 形成 [U-004] | [S-001]/[S-002] |
| 08:50 | 「2次以上失败才进行显示 --，不要显示 NA」 | 失败阈值 2、`N/A` 全文移除（[D-005]） | 异常显示规则定型 | [S-001]/[S-002] |
| 08:53-09:04 | 用户「1 可以采用惰性采样」「先进行测试」「数据一个都没有」「都是 0」 | 实现惰性采样；发现「写后立刻读」被系统缓存吞掉，改用 `FILE_FLAG_NO_BUFFERING` 后数值全部正确（命中/未中/命中率三项实测吻合） | 惰性采样 + 真机验证方法 | [S-001]/[S-002] |
| 12:42 | 惰性采样实现完成 | 「30 秒无显示项查询 → 停止采样」逐条验证；关闭后 22 秒 `rxpcc` 0 次 | 性能开销达标 | [S-002] |
| 13:07-13:22 | 用户要求标签简洁对称 + 自己改配置 | 标签改为 `命中:`/`未中:`；给出用户需自改的 3 行 `config.ini` | 形成 [C-002]/[Q-005] | [S-001]/[S-002] |
| 13:32-13:48 | 「① 覆盖 DLL ② 提交推送 ③ 发 v1.2」+ 清理测试文件 | 删除 26 个测试文件；按显式路径暂存并提交 `7d4c733`；发布 v1.2（x64/x86 哈希校验一致）；部署本地 | v1.2 交付完成 | [S-001]/[S-002]/[S-007] |
| 15:26 | 「检测是否存在bug」 | 代码审查 + `/analyze` + 边界用例，发现并修复 5 处（双睡、千位分隔符、命中率未钳位、`GetTickCount` 溢出、死代码）；解析器 7/7 | v1.2.1 修复集 | [S-001]/[S-002] |
| 15:44-15:47 | 用户提出命中率跳变问题并「选择方案A，但先别执行」 | 给出 A/B/C/D 四方案与权衡；仅把版本标为 v1.2.1（未提交未发布） | 形成 [D-002]/[C-007] | [S-001]/[S-002] |
| 15:51-16:44 | 「先依次执行…发布前做代码检查」+ 睡觉（禁 UAC 操作） | 实现 30 秒滚动窗口（提交 `7b1c759`）；代码检查又发现 3 处加固点并修复（提交 `261213e`）；用例 15/15；工作树干净 | 稳定点 `261213e` | [S-001]/[S-002]/[S-007] |
| 16:45-17:01 | 用户「执行 A1」 | 发布 v1.2.1（Latest，标签→`261213e`），附件下载回来逐字节校验一致；确认本机插件与发布版哈希相同 | 当前线上版本 = v1.2.1 | [S-001]/[S-002] |
| 17:04 | 用户「B4 请你说明一下」 | 说明 B4-a / B4-b 两种门槛方案（推荐 B4-b，1MB），未实施 | 遗留 [Q-003] | [S-001]/[S-002] |
| 17:12 起 | 用户「这个插件还有没有什么值得优化的地方？」 | **无任何回复**：请求在网络层失败（先 502，后 `error sending request`），每 2~7 分钟重试、最多 5 次 | 会话进入不可用状态（[R-001]） | [S-001]/[S-008] |
| 17:15-17:39 | 用户重试并发起「README 文档修复一下」 | 仍全部失败；旧会话上下文 667,507/950,000 token | 遗留 [Q-001]/[Q-004] | [S-001]/[S-008]/[S-009] |
| 18:08 | `README.md` 被修改（mtime） | 工作区出现未提交改动（含 msbuild x86 修订、30 秒窗口描述等） | 见 [F-013]/[F-014]/[Q-002] | [S-010] |
| 18:28-18:30 | 用户「怎么动不了了」；新会话开始排查 | 确认旧会话为网络层失败 + 超大上下文；本机代理 `127.0.0.1:15721` 仍返回 200 | 定位 [R-001] | [S-003]/[S-004] |
| 18:37-18:40 | `TaskBarDynamic.cpp` 被修改（mtime 18:37:49）并重新编译（产物 mtime 18:39-18:40） | 本地 DLL 与 v1.2.1 附件哈希不再一致（[R-009]） | 见 [F-013]/[F-014] | [S-010] |
| 18:42 | 本会话向旧会话发送交接摘要 | 该回合（`01a0cddc`）立即再次失败（18:42:28 / 18:42:53 两次 `error sending request`），仍是 `inProgress` 无输出 | 证实旧会话不可救（[R-001]） | [S-004] |
| 18:5x | 用户确认「你去聊天是无法成功的，上下文已经炸了，没法正常回复你了」 | 决定放弃在旧会话继续 | — | [S-003] |
| 19:0x（本轮） | 用户下达「长对话上下文压缩与交接」指令（要求逐步写入、不要一次性构建） | 正在分批生成 `CONTEXT\_HANDOFF.md` | 当前任务 | [S-003] |
| 2026-09-23（本轮） | 用户确认可拆分并授权执行项目级持久化 | 创建 `AGENTS.md`、`PROTOCOL.md`、`00_INDEX.md`，将 81 KB 旧单文件无损拆分为分域文件，并删除旧单文件及重复归档 | 新对话可通过项目指令自动进入上下文交接流程 | [S-012] |
## 14. 原始来源索引

| 来源 ID   | 位置 | 内容摘要 | 何时需要回查 |
| ------- | -- | ---- | ------ |
| [S-001] | `C:\Users\phrkn\.codex\sessions\2026\09\23\rollout-2026-09-23T06-15-49-01a0cb30-827c-7fe1-848f-e3b5d68dae2e.jsonl` 与 `...\rollout-2026-09-23T08-41-51-01a0cb30-827c-7fe1-848f-e3b5d68dae2e_01a0cbb6-3668-76d3-96e9-1161095ba232.jsonl` 中 `role=user` 条目 | 用户全部原始需求与指令（时间戳为 UTC，+8 为本地） | 需要用户原话、需求边界、拍板结论时 |
| [S-002] | 同 [S-001] 的两个 rollout 文件中 `role=assistant` 条目（`phase=final_answer` 为交付结论，`commentary` 为过程说明） | 助手历次交付、方案说明、验证数据、发布记录 | 需要实现细节、验证数值、方案权衡时 |
| [S-003] | 本会话 rollout：`...\rollout-2026-09-23T18-28-28-01a0cdcf-4495-76c3-8429-770c83733cf9.jsonl`（用户消息） | 本会话用户原话：卡死现象、放弃旧会话、压缩交接指令 | 需要本轮任务口径与用户确认时 |
| [S-004] | 同 [S-003] 的助手回复 + 本会话终端命令输出 | 旧会话卡死排查结论（日志、token 统计、代理存活、重试实测） | 需要卡死证据与复现细节时 |
| [S-005] | `D:\coding\taskbardynamic\README.md`（工作区版本，24,218 B） | 功能/编译/安装/显示项/工作原理/更新日志 | 核对文档口径、定位 L414/L416 待修内容时 |
| [S-006] | `D:\coding\taskbardynamic\taskbardynamic\` 下源码：`config.h/.cpp`、`PrimoCache.h/.cpp`、`TaskBarDynamic.h/.cpp`、`dllmain.cpp`、`DynamicBase.h/.cpp`、`DynamicData.h/.cpp`、`taskbardynamic.vcxproj(.filters)`、`PluginInterface.h`，以及 `D:\coding\taskbardynamic\taskbardynamic.sln` | 权威实现与常量、显示项 ID/标签、平台名映射 | 改代码、核对常量/ID、确认平台名时 |
| [S-007] | 命令输出：`git log --oneline`、`git status --short`、`git diff`、`git rev-parse HEAD`、`git tag --list`、`git diff --stat`（本会话执行，工作目录 `D:\coding\taskbardynamic`） | 提交历史、未提交改动、标签现状 | 核对版本、提交范围、工作区状态时 |
| [S-008] | `C:\Users\phrkn\.codex\logs_2.sqlite`（表 `logs`，只读打开） | 错误原文（502、`error sending request`、`git add -A` 被拒）、重试计数与时间 | 需要错误原文或链路行为证据时 |
| [S-009] | 应用工具读取结果：线程列表/详情、rollout 行级统计（`token_count`、回合状态）、`wait_threads` 快照 | 会话状态（active/idle/inProgress）、上下文 token 数、无工具调用时段 | 需要会话元数据与 token 统计时 |
| [S-010] | 本会话命令输出：`Get-ChildItem`、`Get-FileHash`、`Get-Item`、`netstat -ano`、`Get-Process`、`Invoke-WebRequest http://127.0.0.1:15721/v1/models` | 本机文件哈希/时间戳、部署与备份清单、代理存活 | 核对「本机跑的是哪个版本」、备份与时间线时 |
| [S-011] | `D:\coding\taskbardynamic\_handoff\2026-09-23-traffic-monitor.md`（本会话早先创建的中间交接稿，未跟踪） | 第一次交接摘要（已并入本文件；处置见 [Q-010]） | 需要追溯交接稿来源时 |
| [S-012] | 本会话用户消息与文件生成/校验输出：拆分可行性、效率阈值、项目级持久化、最终执行授权 | 拆分规则、阈值、`AGENTS.md`/`PROTOCOL.md`/索引结构与本次迁移结果 | 需要确认拆分依据、维护流程或迁移审计时 |
